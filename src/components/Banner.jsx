

const Banner = ({ title }) => {
    return (
        <div className="bg-[url('./images/subheader.jpg')] py-10">
            <h2 className="text-3xl text-center text-primary font-bold">{title}</h2>

        </div>
    )
}

export default Banner